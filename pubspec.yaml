name: recipes
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  cupertino_icons: ^1.0.6
  uuid: ^4.4.2
  file: ^7.0.0
  path_provider: ^2.1.4
  path: ^1.9.0
  gap: ^3.0.1
  flutter_ai_toolkit:
    path: ../other/flutter_ai_toolkit
  flutter_markdown: ^0.7.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/recipes_default.json